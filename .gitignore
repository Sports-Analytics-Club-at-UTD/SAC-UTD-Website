# Environment / secrets
.env

# Node
node_modules/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.Python
venv/
env/
.venv/

# Django/SQL
db.sqlite3
staticfiles/
media/
media_hub/migrations
finance/migrations
projects/migrations
requests_hub/migrations
rnd/migrations
events/migrations
core/migrations
accounts/migrations

# Editors / OS
.vscode/
.idea/
.DS_Store

# Migrations note:
# We ARE committing migrations (don't ignore them) — they're the source
# of truth for the DB schema and need to be shared/reviewed in PRs.
